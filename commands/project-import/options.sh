# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Thu Aug  1 13:44:06 PDT 2013
#
#/ usage: rundeck-admin:project-import  --project <>  --url <>  --user <>  --password <> [ --archive <>] [ --retain-uuids <true>] [ --import-executions <true>] 

# _rerun_options_parse_ - Parse the command arguments and set option variables.
#
#     rerun_options_parse "$@"
#
# Arguments:
#
# * the command options and their arguments
#
# Notes:
# 
# * Sets shell variables for any parsed options.
# * The "-?" help argument prints command usage and will exit 2.
# * Return 0 for successful option parse.
#
rerun_options_parse() {

    while [ "$#" -gt 0 ]; do
        OPT="$1"
        case "$OPT" in
            --project) rerun_option_check $# $1; PROJECT=$2 ; shift ;;
            --url) rerun_option_check $# $1; URL=$2 ; shift ;;
            --user) rerun_option_check $# $1; USER=$2 ; shift ;;
            --password) rerun_option_check $# $1; PASSWORD=$2 ; shift ;;
            --archive) rerun_option_check $# $1; ARCHIVE=$2 ; shift ;;
            --retain-uuids) rerun_option_check $# $1; RETAIN_UUIDS=$2 ; shift ;;
            --import-executions) rerun_option_check $# $1; IMPORT_EXECUTIONS=$2 ; shift ;;
            # help option
            -|--*?)
                rerun_option_usage
                exit 2
                ;;
            # end of options, just arguments left
            *)
              break
        esac
        shift
    done

    # Set defaultable options.
    [ -z "$RETAIN_UUIDS" ] && RETAIN_UUIDS="$(rerun_property_get $RERUN_MODULE_DIR/options/retain-uuids DEFAULT)"
    [ -z "$IMPORT_EXECUTIONS" ] && IMPORT_EXECUTIONS="$(rerun_property_get $RERUN_MODULE_DIR/options/import-executions DEFAULT)"
    # Check required options are set
    [ -z "$PROJECT" ] && { echo >&2 "missing required option: --project" ; return 2 ; }
    [ -z "$URL" ] && { echo >&2 "missing required option: --url" ; return 2 ; }
    [ -z "$USER" ] && { echo >&2 "missing required option: --user" ; return 2 ; }
    [ -z "$PASSWORD" ] && { echo >&2 "missing required option: --password" ; return 2 ; }
    # If option variables are declared exportable, export them.

    #
    return 0
}


# Initialize the options variables to null.
PROJECT=
URL=
USER=
PASSWORD=
ARCHIVE=
RETAIN_UUIDS=
IMPORT_EXECUTIONS=


